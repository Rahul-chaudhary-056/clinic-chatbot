services:
  - type: web
    name: clinic-chatbot-1
    runtime: python
    plan: free

    buildCommand: pip install -r requirements.txt

    startCommand: gunicorn whatsapp_bot:app

    envVars:
      - key: PYTHON_VERSION
        value: 3.12.10